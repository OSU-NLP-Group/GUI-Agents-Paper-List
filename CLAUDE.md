# CLAUDE.md - GUI Agents Paper List

## Project Overview

This is a curated paper list repository for GUI Agent research, covering web, mobile, desktop, and search-based agents. It tracks ~600+ papers with structured metadata.

## Repository Structure

- `update_template_or_data/update_paper_list.md` — **Source of truth** for all paper entries. Edit this file to add/modify papers.
- `update_template_or_data/update_readme_template.md` — README template with `{{placeholders}}` filled by the workflow.
- `update_template_or_data/utils/scripts/sort_by_date.py` — Main script that:
  - Parses `update_paper_list.md`
  - Sorts papers by date (newest first) and normalizes date formats
  - Generates `ALL_PAPERS.md` (complete list) and `recent_paper_list.md` (most recent 500 for README)
  - Generates `paper_by_env/`, `paper_by_key/`, `paper_by_author/` files
  - Generates `keyword_grouping.md`, `author_grouping.md`
  - Generates keyword wordcloud image
- `ALL_PAPERS.md` — Auto-generated complete paper list at repo root. **Do not edit directly.**
- `update_template_or_data/keyword_grouping.md` / `author_grouping.md` — Auto-generated, inserted into README by workflow.
- `paper_by_env/` — Papers grouped by environment (Web, Mobile, Desktop, GUI, Search, Misc). Auto-generated.
- `paper_by_key/` — Papers grouped by keyword. Auto-generated.
- `paper_by_author/` — Papers for top 20 authors. Auto-generated.
- `README.md` — Auto-generated by GitHub Actions workflow. Shows only the most recent 500 papers (GitHub truncates rendering at ~512KB). **Do not edit directly.**
- `.github/workflows/main.yml` — Workflow triggered by changes to `update_paper_list.md`, `sort_by_date.py`, or `update_readme_template.md`.

## How to Add a Paper

Edit `update_template_or_data/update_paper_list.md` and add an entry in this exact format:

```
- [title](paper link)
    - author1, author2, author3
    - 🏛️ Institutions: Institution1, Institution2
    - 📅 Date: Oct 30, 2024
    - 📑 Publisher: ICLR 2025
    - 💻 Env: [Web]
    - 🔑 Key: [framework], [dataset], [benchmark], [SomeAbbreviation]
    - 📖 TLDR: Brief summary of the paper.
```

**Important formatting rules:**
- Indentation is exactly 4 spaces for each sub-field.
- Each entry must have exactly these 7 sub-fields in this order.
- There must be a blank line between entries.
- The TLDR must end with a newline (the regex parser depends on this).

### Field Specifications

**Paper Link:** Prefer project homepage > arxiv link > conference link. Only one link.

**Authors:** List directly without a key identifier (e.g., `author1, author2`). Include all authors.

**Institutions:** Use common abbreviations (e.g., OSU, MIT, CMU). Use "Unknown" if not found.

**Date:** First publication date (not update date). Use canonical format `Month DD, YYYY` (e.g., `October 30, 2024`). The script will normalize other formats (like `2024-10-30`) to this canonical form. Include day if known. Year-month is acceptable (e.g., `November 2024`) — the script will default to the last day of that month.

**Publisher:** Conference/journal abbreviation with year (e.g., `ICML 2024`, `NeurIPS 2025`). Use `arXiv` if only on arXiv. Use `Unknown` if unknown.

**Env (Environment):**
- `[Web]` — Browser-based GUI agents interacting with real web pages
- `[Mobile]` — Android/iOS GUI agents
- `[Desktop]` — Computer-use agents (Windows/Mac/Linux)
- `[GUI]` — Spans multiple GUI environments, or general GUI research
- `[Search]` — Papers purely using Search APIs for web browsing without actual GUI interaction
- `[Misc]` — General domain research with important applications to GUI agents
- Multiple envs are allowed: `[Web], [Search]` for papers that cover both browser-based and search-based approaches

**Key (Keywords):**
- Each keyword wrapped in `[]`, comma-separated
- Include `[model]` if a new model is trained (not just using GPT-4 etc.)
- Include `[framework]` if a new agent framework is proposed
- Include `[dataset]` if a new dataset is created and published
- Include `[benchmark]` if a new benchmark is established (add `[dataset]` too if it includes training data)
- Include the paper's commonly used abbreviation (e.g., `[SeeAct]`, `[OSWorld]`)
- Include primary research focus/innovation keywords
- Avoid overly generic tags like `[web navigation]`, `[LLM agent]`, `[GUI agent]`, `[multimodal]` — these are already implied by the paper list's scope and the Env field

**TLDR:** 1-3 sentences summarizing the paper's contribution. Focus on what it does and what it achieves.

### Searching for Paper Information

When asked to add papers, search the internet for:
- Full author list
- Exact first publication date
- Institutions (use abbreviations)
- Whether it has an arXiv version
- Whether it's accepted at a conference/journal
- Abstract and main contributions

## Workflow Details

The GitHub Actions workflow (`.github/workflows/main.yml`):
1. Triggers on pushes that modify `update_paper_list.md`, `sort_by_date.py`, `update_readme_template.md`, or `main.yml`
2. Runs `sort_by_date.py` to sort papers and generate grouped files
3. Inserts `keyword_grouping.md`, `author_grouping.md`, and sorted paper list into the README template
4. Overwrites `README.md` with the assembled result
5. Commits and pushes auto-generated files

## Development Notes

- The regex parser in `sort_by_date.py` is strict about the entry format. Malformed entries will be silently dropped.
- Environment field supports multi-env (e.g., `[Web], [Search]`) — the regex captures `\[.*?\](?:, \[.*?\])*`.
- Date strings are normalized to `Month DD, YYYY` format on each run. Month-only dates get the last day of the month.
- GitHub truncates README rendering at ~512KB. The README only includes the most recent 500 papers; the full list is in `ALL_PAPERS.md`.
- `seaborn` is listed as a pip dependency in the workflow but currently unused (kept for potential future use).
- The `safe_execute` decorator silently swallows errors and logs them to `update_template_or_data/logs/error.log`. Check this file if entries seem to disappear.
- Only the top 20 keywords and top 20 authors get their own grouped files.
- `README.md`, `ALL_PAPERS.md`, and all files under `paper_by_*/` are auto-generated — always edit the source files instead.
